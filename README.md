# SSR-server-side-rebdering-for-SEO
 Background: Our current storefront is a React (client-side) app, which means search engine crawlers like Google see an empty page without any product content. To fix this SEO issue, we've built a separate Next.js app that server-renders Product Detail Pages so crawlers receive the full product content immediately.
